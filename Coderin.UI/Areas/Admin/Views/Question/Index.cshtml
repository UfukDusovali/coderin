@model IEnumerable<Coderin.Entity.Question>

@{
    ViewBag.Title = "Index";
    Layout = "~/Areas/Admin/Views/Shared/AdminLayout.cshtml";
}

<h2>Soru Yönetimi</h2>

<p>
    @Html.ActionLink("Soru Oluştur", "Create")

<section class="content">
    <div class="row">
        <div class="col-xs-12">
            <div class="box">
                <div class="box-header">
                    <h3 class="box-title">Sorular</h3>
                </div><!-- /.box-header -->
                <div class="box-body">
                    <table id="example2" class="table table-bordered table-striped data-table">
                        <thead>
                            <tr>
                                <th>Kullanıcı Adı</th>
                                <th>Name</th>
                                <th>İşlemler</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var item in Model)
                            {
                                <tr>
                                    <td>@item.User.Name @item.User.Surname</td>
                                    <td>@item.Name</td>
                                    <td>
                                        <a href="~/Admin/Question/Edit/@item.Id">Güncelle</a> |
                                        <a href="~/Admin/Question/Details/@item.Id">Detay</a> |
                                        <a href="~/Admin/Question/Delete/@item.Id" onclick="return window.confirm('Soru Silinecek Emin Misiniz!');">Sil</a>
                                </tr>
                            }
                        </tbody>

                    </table>
                </div>
            </div>
        </div>
    </div>
</section>